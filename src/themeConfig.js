import { createTheme } from "@mui/material";

 export const customTheme = createTheme({
    palette:{
        primary: {
            main: "#B393D6",
            // secondary: "crimson"
        },
        secondary: {
            main: "#8E6CB4"
        }
    }
})